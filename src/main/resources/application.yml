server:
  port: 9001

spring:
  application:
    name: Raj-School

  jackson:
    date-format: yyyy-MM-dd'T'HH:mm:ss

  datasource:
    url: jdbc:mysql://localhost:3306/Student_Records
    username: root
    password: Rajkumar@123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect  # Use MySQLDialect instead of MySQL8Dialect
